using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FollowStateRanged 
{
    
    // private Vector3 CheckSurroundings (Vector3 v, RangedEnemy p){
    //     if (p.Left)
    //         v.x = Mathf.Clamp (v.x, p.Left.point.x + p.len, Mathf.Infinity);
    //     if (p.Right)
    //         v.x = Mathf.Clamp (v.x, Mathf.NegativeInfinity, p.Right.point.x - p.len);
        
    //     return v;
    // }
}
