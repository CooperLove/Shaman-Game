using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Handle_UI_Input : MonoBehaviour
{
    //[SerializeField] private GameObject inventory = null;
    //[SerializeField] private GameObject charMenu = null;

    // Update is called once per frame
    void Update()
    {
        
        // if (Input.GetKeyDown(KeyCode.C))
        //     charMenu.SetActive(!charMenu.activeInHierarchy);
        // if (Input.GetKeyDown(KeyCode.B) || Input.GetKeyDown(KeyCode.I))
        //     inventory.SetActive(!inventory.activeInHierarchy);
    }
}
